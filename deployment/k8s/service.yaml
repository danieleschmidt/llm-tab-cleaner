apiVersion: v1
kind: Service
metadata:
  name: llm-tab-cleaner-service
  namespace: llm-tab-cleaner
  labels:
    app: llm-tab-cleaner
spec:
  selector:
    app: llm-tab-cleaner
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
  type: ClusterIP
