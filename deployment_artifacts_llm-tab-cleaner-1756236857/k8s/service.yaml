apiVersion: v1
kind: Service
metadata:
  name: llm-tab-cleaner-service
  namespace: llm-tab-cleaner
spec:
  selector:
    app: llm-tab-cleaner
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: ClusterIP
